/**
 * Enumeration for game end states, used in the `GameEndDTO` to represent the outcome of a game.
 *
 * @enum {number}
 * @property {WIN} Indicates a win condition.
 * @property {DRAW} Indicates a draw condition.
 */
export enum EGameEndDTO {
    WIN,
    DRAW,
}
